dataset:
  data_dir: "data/cora/"

  train:
    class: data_source.cora.Cora
    params:
      data_dir: ${dataset.data_dir}
      mode: train

  validation:
    class: data_source.cora.Cora
    params:
      data_dir: ${dataset.data_dir}
      mode: val

  test:
    class: data_source.cora.Cora
    params:
      data_dir: ${dataset.data_dir}
      mode: test